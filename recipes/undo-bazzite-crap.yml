modules:
  - type: rpm-ostree
    install:
      - plasma-discover # For the love of god, why does Bazzite replace great basic packages
      - PackageKit # needed for discover
      # - plasma-discover-packagekit # maybe needed for Discover? Seems to cause a bug and prevent Discover from opening
    remove:
      - ptyxis # ptyxis is so much worse than konsole
      - bazaar
      - krunner-bazaar
